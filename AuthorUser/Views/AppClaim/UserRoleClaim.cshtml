@model IEnumerable<AuthorUser.Models.Claims.ClaimViewModel>
@{
    ViewBag.Title = "UserRoleClaim";
}

<h2>UserRoleClaim</h2>

<p>
    @Html.ActionLink("Create New", "CreateUserRoleClaim")
</p>
<table class="table">
    <tr>
        <th>
            Email
        </th>
        <th>
            Claim Type
        </th>
        <th>
            Claim Value
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Email
            </td>
            <td>
                @item.ClaimType
            </td>
            <td>
                @item.ClaimValue
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item. }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
            </td>
        </tr>
    }

</table>

